import styled from "styled-components"

export const Section = styled.div`
  background: linear-gradient(180deg, #074d6c 0%, #0c3040 100%);
  padding: 90px 30px;
  
  @media screen and (max-width: 768px) { padding: 30px 0; }
`
